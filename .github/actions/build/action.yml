name: Build distribution packages
description: Builds wheel and sdist with uv and uploads them as an artifact

runs:
  using: composite
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@v8
      with:
        enable-cache: true

    - name: Build
      shell: bash
      run: uv build

    - name: Upload distributions
      uses: actions/upload-artifact@v4
      with:
        name: python-package-distributions
        path: dist/
